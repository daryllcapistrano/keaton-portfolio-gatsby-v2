import { makeStyles } from "@material-ui/core/styles"

export default makeStyles(() => ({
  root: {
    maxWidth: "100%",
  },
  imageWrapper: {
    width: "100%",
  },
  linkWrapper: {
    display: "flex",
    flexDirection: "column",
  },
}))
